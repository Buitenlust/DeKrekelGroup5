@model DeKrekelGroup5.ViewModel.MainViewModel
<div id="heading">
    @Model.GebruikerViewModel.Username Logout:
</div>

<div id="contentLogin">

    @if (@Model.GebruikerViewModel.Username == "Beheerder")
    {
        <img src="~/Images/admin.png" class="tinyImg" />
    }
    @if (@Model.GebruikerViewModel.Username == "Bibliothecaris")
    {
        <img src="~/Images/guest.png" class="tinyImg" />
    }

    @using (Ajax.BeginForm("Logout",
                        new AjaxOptions
                        {
                            InsertionMode = InsertionMode.Replace,
                            HttpMethod = "Post",
                            UpdateTargetId = "LogoutDiv"
                        }
                        ))
    {
        <div id="LogoutDiv">
            @Html.Partial("_LogoutPartial", Model)
        </div>
    }
</div>